# PassWord_Generator
It generates secure 16-character passwords (containing uppercase and lowercase letters, digits, and symbols) and displays them on screen, automatically rotating them every 5–15 seconds until the user presses Enter; at that point, it stops and displays the last generated password.
